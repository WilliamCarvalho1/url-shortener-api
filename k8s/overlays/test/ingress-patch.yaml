apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: url-shortener
spec:
  rules:
    - host: url-shortener-test.example.com
  tls:
    - hosts:
        - url-shortener-test.example.com
      secretName: url-shortener-test-tls