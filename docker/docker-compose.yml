services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=url_shortener'
      - 'POSTGRES_PASSWORD=password'
      - 'POSTGRES_USER=admin'
    ports:
      - '5432:5432'